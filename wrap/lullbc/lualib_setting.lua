local lualib_setting = {
    use_setting = false, -- If use_setting is False, compile system will auto detect lua 
    lua_path = {"", "", ""}, -- luapath setting, 1: include path, 2: library path, 3: library name
}

return lualib_setting
